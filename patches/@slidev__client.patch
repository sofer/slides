diff --git a/logic/slides.ts b/logic/slides.ts
index cb156a18af745ff4c2028bc6515683ec2e45c277..14c06191d9d407e1939dbff54d55cd139c37ecd7 100644
--- a/logic/slides.ts
+++ b/logic/slides.ts
@@ -21,7 +21,11 @@ export function getSlidePath(
     route = getSlide(route)!
   const no = route.meta.slide?.frontmatter.routeAlias ?? route.no
   const path = exporting ? `export/${no}` : presenter ? `presenter/${no}` : `${no}`
-  return `${import.meta.env.BASE_URL}${path}`
+  // Patched: return a root-relative route path. The router history already
+  // applies BASE_URL, so prefixing it here (upstream PR #2562) doubles the base
+  // and breaks navigation under a subpath (e.g. /slides/slides/2). See
+  // https://github.com/slidevjs/slidev/issues/248
+  return `/${path}`
 }
 
 export function useIsSlideActive() {

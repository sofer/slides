{
  "name": "slides",
  "private": true,
  "scripts": {
    "dev": "slidev perceptrons.slides.md",
    "build": "slidev build perceptrons.slides.md --base /slides/ --out dist"
  },
  "devDependencies": {
    "@slidev/cli": "^52.16.0",
    "@slidev/theme-default": "^0.25.0"
  }
}
